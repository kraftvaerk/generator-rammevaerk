{
    "scripts": {
        "preinstall": "npm install jquery bootstrap@4.0.0-beta @fancyapps/fancybox github:kraftvaerk/cookie-consent --save --save-exact",
        "gulp": "gulp",
        "lint": "gulp scripts:lint && gulp styles:lint",
        "watch": "gulp watch",
        "test": "echo \"Error: no test specified\" && exit 1"
    }
}
