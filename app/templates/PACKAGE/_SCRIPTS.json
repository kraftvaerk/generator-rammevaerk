{
    "scripts": {
        "install": "jspm install --production",
        "lint": "gulp scripts:lint && gulp styles:lint",
        "watch": "gulp watch",
        "test": "echo \"Error: no test specified\" && exit 1",
        "jspm": "jspm",
        "gulp": "gulp"
    }
}
