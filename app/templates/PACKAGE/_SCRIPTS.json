{
    "scripts": {
        "install": "jspm install && gulp scripts:pathfix",
        "jspm": "jspm",
        "gulp": "gulp",
        "lint": "gulp scripts:lint && gulp styles:lint",
        "watch": "gulp watch",
        "test": "echo \"Error: no test specified\" && exit 1"
    }
}